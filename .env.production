# Production environment — baked at build time by Next.js.
# Fetch mode: every repository talks to the Sprint 18 backend.
NEXT_PUBLIC_API_MODE=fetch
NEXT_PUBLIC_API_BASE_URL=https://fuurin.reiyuura.pw/api
NEXT_PUBLIC_API_VERSION=v1
NEXT_PUBLIC_API_TIMEOUT_MS=15000
